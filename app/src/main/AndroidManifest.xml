<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.artmil.mygallery">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">


        <activity android:name=".lesson4.task2.RetorfitActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".lesson4.task1.GsonActivity">
            <!--<intent-filter>

            </intent-filter>-->
        </activity>

        <!--<activity android:name=".lesson3.view.ActivityLesson3Task1">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".lesson3.view.ActivityLesson3Task2">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".lesson2.view.ActivityLesson3_3">
            <intent-filter>

            </intent-filter>
        </activity>

        <activity android:name=".lesson2.view.ActivityLesson2_2">
            <intent-filter>

            </intent-filter>
        </activity>

        <activity android:name=".view.MainActivity">
            <intent-filter>

            </intent-filter>
        </activity>-->

    </application>

</manifest>